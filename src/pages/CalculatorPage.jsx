import React from 'react';
import Calculator from '../components/Calculator/Calculator.jsx';

export default function CalculatorPage() {
  // Esta página podría tener más elementos en el futuro,
  // como un título propio o instrucciones adicionales.
  // Por ahora, solo renderizará el componente de la calculadora.
  return <Calculator />;
}